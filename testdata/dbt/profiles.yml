default:
  target: ${INPUT_DBT_TARGET}
  outputs:
    snowflake:
      type: snowflake
      account: "{{ env_var('ACCOUNT') }}"
      user: "{{ env_var('USERNAME') }}"
      password: "{{ env_var('PASSWORD') }}"
      database: "{{ env_var('DATABASE') }}"
      schema: "{{ env_var('SCHEMA') }}"
      warehouse: "{{ env_var('WAREHOUSE') }}"
      